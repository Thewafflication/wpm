\documentclass[letterpaper]{article}
\usepackage[letterpaper,margin=1in]{geometry}
\input{wsp/testing/test-case-library.tex}
\begin{document}
\def\TCID{TC-0029}
\def\TCTitle{Required and excess operand shapes}
\def\TCPurpose{Verify the basic CP-01B operand-shape partitions of REQ-0014.007 before durable initialization.}
\def\TCPriority{\TCPriorityHigh}
\def\TCRequirementRef{REQ-0014.007}
\def\TCDesignRef{ADR-0010 human-presentation boundary}
\def\TCFeatureRef{Central operand-shape validator and \texttt{tests/tc-0029-operand-shapes.ps1}}
\def\TCTestTechnique{Boundary-value and equivalence-partition negative testing.}
\def\TCPreconditions{A native WPM build and nonexistent isolated data root.}
\def\TCEnvironment{Native Windows x86, x64, or ARM64 with PowerShell capture.}
\def\TCAssumptions{Nested action semantics for repo, key, trust, and config remain command-level validation.}
\def\TCInputData{Missing required and excess operands for simple-shape public commands.}
\def\TCInitialState{Point WPM at a disposable data-root path that does not exist.}
\def\TCProcedure{Exercise every minimum/maximum boundary and inspect error, narrow usage, help direction, and state.}
\def\TCExpectedResult{Each invalid shape fails before durable initialization with only relevant guidance.}
\def\TCPostConditions{Remove disposable state and retain configured evidence.}
\TCTemplate
\end{document}
